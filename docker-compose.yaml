services:
  rust:
#    user: 1000:1000
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /work
    volumes:
      - .:/work
    environment:
      CARGO_HOME: /work/.cache/cargo
#      RUSTUP_HOME: /work/.cache/rustup
    command: cargo build
