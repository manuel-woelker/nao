[package]
name = "nao"
version = "0.1.0"
edition = "2024"
description = "CLI for the nao task runner"
license = "MIT"
repository = "https://github.com/manuel-woelker/nao"

[dependencies]
nao-base = { path = "../base" }
nao-pal = { path = "../pal" }
nao-recipe = { path = "../recipe" }
xflags = { workspace = true }

[dev-dependencies]
expect-test = { workspace = true }
